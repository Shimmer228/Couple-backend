DATABASE_URL="postgresql://postgres:postgres@localhost:5433/coupleapp?schema=public"
JWT_SECRET="replace-with-a-strong-secret"
NODE_ENV="development"
PUBLIC_BASE_URL="http://localhost:5000"
# Optional: comma-separated web origins if you want stricter CORS in production.
# Native Android clients usually send no Origin header, which remains allowed.
CORS_ORIGINS="http://localhost:3000,https://your-frontend.example.com"
